<%= simple_form_for @bulletin do |f| %>
  <p><%= f.input :title %></p>
  <p><%= f.input :description, as: :text  %></p>
  <p><%= f.input :category_id, collection: Category.all %></p>
  <p><%= f.file_field :image %></p>
  <p><%= f.button :submit, class: "btn btn-primary" %></p>
<% end %>
